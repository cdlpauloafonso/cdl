# Copie para .env no servidor (NÃO commitar .env nem .env_api):
#   cp .env_api.example .env && chmod 600 .env && nano .env

NODE_ENV=production
PORT=4000

FRONTEND_URL=https://www.cdlpauloafonso.com

# openssl rand -hex 48
JWT_SECRET=

# JSON inteiro da Service Account em UMA linha, entre aspas simples
FIREBASE_SERVICE_ACCOUNT_JSON=''

DATABASE_URL="postgresql://disabled:disabled@127.0.0.1:5432/disabled?schema=public"
